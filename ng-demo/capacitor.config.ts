import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krishpro.captionimages',
  appName: 'caption-images',
  webDir: 'dist/caption-images',
  bundledWebRuntime: false
};

export default config;
